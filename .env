# ── MongoDB Atlas ────────────────────────────────────────────
# Replace <username>, <password>, <cluster> with your Atlas details
MONGO_URI=mongodb+srv://sisimwo:sethsisimwo@cluster0.gj1ewyc.mongodb.net/sisimwo?retryWrites=true&w=majority&appName=Cluster0

# ── Server ────────────────────────────────────────────────────
PORT=5000

# Allow requests from your frontend (use * for local dev)
FRONTEND_URL=http://127.0.0.1:5500

# ── Email (SMTP via Gmail) ────────────────────────────────────
# In Gmail: Settings → Security → App Passwords → generate one
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=hello@sisimwoestate.co.ke
SMTP_PASS=your_16_char_app_password

# ── Admin notification email ──────────────────────────────────
ADMIN_EMAIL=martin@sisimwoestate.co.ke
